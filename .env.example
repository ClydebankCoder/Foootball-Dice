# Supabase is entirely optional for the MVP — the game is fully playable
# without it, saving careers to localStorage instead.
#
# Copy to `.env.local` and fill in to enable the client and email auth, then
# apply `supabase/schema.sql` to your project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
